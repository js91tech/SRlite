{
  "name": "roadside-radar",
  "version": "0.1.0",
  "private": true,
  "description": "Roadside dispatch lead radar for Marietta GA and configurable territories",
  "workspaces": [
    "server",
    "client"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "db:migrate": "npm run db:migrate -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
